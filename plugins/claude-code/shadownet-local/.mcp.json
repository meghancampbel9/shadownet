{
  "mcpServers": {
    "shadownet": {
      "type": "http",
      "url": "${SHADOWNET_SIDECAR_BASE_URL}/u/${SHADOWNET_SHADOWNAME}/mcp",
      "headers": {
        "Authorization": "Bearer ${SHADOWNET_TOKEN}"
      }
    }
  }
}
